// Problem 5
const countOccurrences = <T>(arr: T[], input: T) => {
    return arr.filter((item) => item === input).length
}
export default countOccurrences; // Uncomment this line when done writing the function
